on: [push]

jobs:
  scanner:
    runs-on: ubuntu-latest
    name: Scan
    steps:
       - name: Checkout
         uses: actions/checkout@v2
       - name: Malware Scanner
         uses: malware-scanner@v1
         with:
             directories: ./...
             options: -rf
     